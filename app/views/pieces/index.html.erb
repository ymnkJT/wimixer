<h1>楽曲一覧</h1>

<% @pieces.each do |piece| %>
  <p>曲名:<%= piece.title %></p>
  <p>作曲:<%= piece.composer %></p>
  <p>編曲:<%= piece.arranger %></p>
  <p>出版:<%= piece.publisher %></p>
<% end %>

<%= link_to "新規に楽曲を登録しますか？", new_piece_path %>